{
  "name": "iqbal-jutt-netlify-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate --schema prisma/schema.prisma && npx prisma db push --schema prisma/schema.prisma && cd ../frontend && npm install && npm run build",
    "postinstall": "npx prisma generate --schema backend/prisma/schema.prisma",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev"
  },
  "dependencies": {
    "@netlify/functions": "^3.1.10",
    "serverless-http": "^3.2.0",
    "express": "^5.2.1",
    "cors": "^2.8.6",
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "typescript": "^6.0.3"
  }
}
